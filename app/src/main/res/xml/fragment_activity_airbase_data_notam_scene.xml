<?xml version="1.0" encoding="utf-8"?>
<MotionScene xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:motion="http://schemas.android.com/apk/res-auto"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ConstraintSet android:id="@+id/start">
    </ConstraintSet>

    <ConstraintSet android:id="@+id/end">
        <Constraint
            android:id="@id/raw_notam"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">
        </Constraint>
        <Constraint
            android:id="@+id/decoded_notam"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            motion:layout_constraintTop_toBottomOf="@id/raw_notam"
            motion:layout_constraintStart_toStartOf="parent">
            <Transform android:translationY="32dp"/>
            <PropertySet android:alpha="0.0" />
        </Constraint>
    </ConstraintSet>

    <Transition
        android:id="@+id/startToEnd"
        app:constraintSetEnd="@id/end"
        app:constraintSetStart="@+id/start"
        app:duration="1000">
        <OnSwipe
            app:dragDirection="dragDown"
            motion:onTouchUp="autoComplete"
            app:touchAnchorId="@id/decoded_notam"
            app:touchAnchorSide="bottom"
            app:touchRegionId="@id/decoded_notam" />
        <KeyFrameSet>
            <KeyAttribute
                android:alpha="1.0"
                app:framePosition="50"
                app:motionTarget="@id/decoded_notam" />
        </KeyFrameSet>
    </Transition>
</MotionScene>